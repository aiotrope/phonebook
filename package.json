{
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^6.6.5",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:prod": "rm -rf build && cd frontend && rm -rf build && npm run build:prod && cp -r build ../",
    "build:dev": "rm -rf build && cd frontend && rm -rf build && npm run build && cp -r build ../"
  }
}
