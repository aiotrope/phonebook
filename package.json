{
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:frontend": "rm -rf build && cd frontend && rm -rf build && npm run build && cp -r build ../" 

  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
